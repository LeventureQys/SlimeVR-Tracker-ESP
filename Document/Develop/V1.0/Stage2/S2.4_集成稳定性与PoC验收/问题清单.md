# V1.0-S2.4 问题清单

## 1. 已关闭问题

| ID | 优先级 | 状态 | 结论与依据 |
| --- | --- | --- | --- |
| `Q-S2.4-03` | P1 | 已关闭（暂定） | 30 分钟无界增长判定暂定为：进程内存增量 `< 10 MB` 且句柄数变化 `< 5%`；执行真实测试时可复核调整。已写入 `稳定性测试记录.md` 断言。 |
| `Q-S2.4-04` | P1 | 已关闭 | 发送率允许误差：配置值 `±10%`。已写入稳定性模板断言。 |
| `Q-S2.4-05` | P2 | 已关闭 | 网络设置放入主窗口独立“SlimeVR 输出”分组，安装旋转用独立对话框；objectName 前缀 `slime*`/`mount*`，由 `test_slimevr_integration` 与 `test_main_window` 查找。 |
| `Q-S2.4-06` | P2 | 已关闭 | SlimeVR 输出默认禁用；勾选“启用”并点击“应用设置”后生效，可完全回退原工具行为。 |
| `Q-S2.4-07` | P2 | 已关闭 | 配置迁移键组 `slimevr/*`：enabled/discoveryMode/host/port/handshakeIntervalMs/connectionTimeoutMs/sendRateHz/gloveSide/deviceId/mount0..5；缺失或非法键逐键回退默认（安装旋转逐路回退单位值）。 |

## 2. 未关闭问题（真实环境门禁）

| ID | 优先级 | 状态 | 问题 | 关闭条件 |
| --- | --- | --- | --- | --- |
| `Q-S2.4-01` | P1 | 待确认 | 30 分钟稳定性验收使用的电脑、串口和 Server 版本 | 环境表填写完成并执行 `稳定性测试记录.md` |
| `Q-S2.4-02` | P1 | 待确认 | 左手和右手真实硬件是否都可用于验收 | 两侧实测，或用户批准单侧硬件 + 另一侧软件验证 |

## 3. 验收状态

- 软件实现与自动化测试：完成（全量 11/11 测试目标通过）；
- 真实 Server 六节点注册、真实硬件动作验证、30 分钟长稳：未执行（无硬件），步骤见 `用户操作说明.md` 与 `稳定性测试记录.md`；
- 上述未执行项关闭前，阶段 2 真实环境验收保持未完成。
