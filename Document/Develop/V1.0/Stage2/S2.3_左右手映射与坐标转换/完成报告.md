# V1.0-S2.3 完成报告

## 1. 版本目标

完成左右手节点映射、四元数顺序、坐标语义和每路固定安装旋转，并以确定性测试锁定公式。

## 2. 修改文件

新增（`Document/Research/手套IMU解算MVP/`）：

- `src/slimevr_coordinate_transform.h/.cpp`：四元数坐标变换与安装旋转管理；
- `tests/test_slimevr_coordinate_transform.cpp`。

修改：

- `src/slimevr_settings.h/.cpp`：新增六路 `mountings` 与校验；
- `src/slimevr_settings_store.h/.cpp`：安装旋转持久化（每路独立回退）；
- `src/slimevr_pose_adapter.h/.cpp`：接入坐标变换；
- `src/slimevr_pose_sender.h/.cpp`：`setMountings` 透传；
- `CMakeLists.txt`：新测试目标。

文档：

- `坐标系设计.md`：源/目标坐标定义与公式推导；
- `动作验证记录.md`：左右手验证表（真实硬件未接入，全部待执行）。

## 3. 坐标结论（问题清单已关闭项）

- 源语义：Madgwick 原版主动旋转（参考系→当前传感器系），右手系，由 `test_madgwick_filter::A03` 锁定；
- 目标语义：SlimeVR 节点轴 X=左、Y=上、Z=前（固件 `sensor.h` 注释），右手系，Server 挂载校准建立参考；
- 转换公式：`q_node = conj(q_mount) · q_source · q_mount`；identity 安装直通；
- 左右手不改变四元数公式，只改变 tracker type 与位置枚举；
- 网络四元数固定 `x,y,z,w`。

## 4. 测试结果

```text
test_slimevr_coordinate_transform    9/9 通过
全量 CTest                         10/10 目标通过
```

覆盖：identity 直通、sandwich 公式、手侧无关、非法安装拒绝、每路独立、设置存取、损坏键每路回退、适配器应用安装旋转。

## 5. 遗留问题（硬件门禁）

- `Q-S2.3-04`：六路 PCB 实际轴一致性待真实硬件单轴动作验证；
- `Q-S2.3-05`：左右手物理装配差异待双手硬件验证；
- `Q-S2.3-06`：指尖安装段是否确为 distal 待硬件结构确认；
- 真实动作验证未完成前，“左右手正向动作符合验证表”验收项保持未关闭。
