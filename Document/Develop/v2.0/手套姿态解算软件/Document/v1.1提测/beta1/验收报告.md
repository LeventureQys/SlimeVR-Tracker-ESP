# 验收报告 —— v1.1 beta1

- 版本：v1.1 beta1（目录 `Document/v1.1提测/beta1/`）
- 验收依据：`验收文档.md`
- 验收时间：2026-08-19

---

## 验收总览

| 层级 | 状态 | 说明 |
| --- | --- | --- |
| 第一层：编译测试 | ⚠️ 阻塞 | 本机无 Qt6 + MSVC 编译环境 |
| 第二层：启动测试 | ⚠️ 阻塞 | 依赖编译 |
| 第三层：功能完整性测试 | ⚠️ 阻塞 | 依赖启动 |
| 第四层：整体测试 | ⚠️ 阻塞 | 依赖启动 |

**阻塞原因**：开发环境（DeepSeek Harness）无 Qt6 6.8+ SDK 和 MSVC 编译器。CMake 4.3.2 可用但 `find_package(Qt6)` 会失败。需要在有 Qt6 开发环境的机器上执行编译和启动测试。

---

## 代码层面验收（已完成）

### 1. 渲染修复验证

| # | 检查项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1.1 | vertex shader 传递 vWorldPosition | ✅ 通过 | `hand_render_widget.cpp:400,414` |
| 1.2 | fragment shader Blinn-Phong specular | ✅ 通过 | `hand_render_widget.cpp:429-432` |
| 1.3 | overlay 禁用深度测试 | ✅ 通过 | `hand_render_widget.cpp:720` |
| 1.4 | overlay 恢复深度测试 | ✅ 通过 | `hand_render_widget.cpp:747` |
| 1.5 | 点大小 8.0→6.0 | ✅ 通过 | `hand_render_widget.cpp:741` |

### 2. UI 布局修复验证

| # | 检查项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 2.1 | 窗口尺寸 1420→1600 | ✅ 通过 | `main_window.cpp:57` |
| 2.2 | 左侧 dock 最小宽度 200px | ✅ 通过 | `main_window.cpp:95` |
| 2.3 | 右侧 dock 最小宽度 340px | ✅ 通过 | `main_window.cpp:161` |
| 2.4 | QScrollArea 包裹 | ✅ 通过 | `main_window.cpp:162-167` |
| 2.5 | 状态栏标签最小宽度 | ✅ 通过 | `main_window.cpp:176-178` |

### 3. 录制功能验证

| # | 检查项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 3.1 | DataRecorder 类完整性 | ✅ 通过 | `data_recorder.h` 57 行，`data_recorder.cpp` 132 行 |
| 3.2 | 状态机 Idle→Recording→Paused | ✅ 通过 | `data_recorder.cpp:20-71` |
| 3.3 | CSV 格式 16 列 | ✅ 通过 | `data_recorder.cpp:119` |
| 3.4 | 文件夹创建 | ✅ 通过 | `data_recorder.cpp:30` |
| 3.5 | UI 按钮+信号连接 | ✅ 通过 | `main_window.cpp:291-405` |
| 3.6 | 时间显示 mm:ss | ✅ 通过 | `main_window.cpp:400-402` |
| 3.7 | CMake 配置 | ✅ 通过 | `CMakeLists.txt:45,57-64` |
| 3.8 | protocolParser→recorder 连接 | ✅ 通过 | `main_window.cpp:404-405` |

### 4. 不动保护验证

| 文件 | 是否修改 | 说明 |
| --- | --- | --- |
| `wit_protocol_parser.h/cpp` | ❌ 未修改 | 正确 |
| `imu_data.h` | ❌ 未修改 | 正确 |
| `wit_ble_manager.h/cpp` | ❌ 未修改 | 正确 |
| `pose_solver.h/cpp` | ❌ 未修改 | 正确 |
| `single_imu_finger_controller.h/cpp` | ❌ 未修改 | 正确 |
| `model_data.h` | ❌ 未修改 | 正确 |

### 5. 集成修复

| 问题 | 修复 |
| --- | --- |
| CMakeLists.txt 源文件重复添加 | 移除直接添加，保留库链接方式 |
| main_window.cpp 重复 `#include <QFrame>` | 合并为单个 include |

---

## 待在目标机执行的测试

在有 Qt6 6.8+ 和 MSVC 的开发机上执行：

```bash
# 编译
cd archived
cmake -B build -S . -DCMAKE_BUILD_TYPE=Debug
cmake --build build --config Debug

# 启动测试
./build/bin/hand_rig_demo.exe
```

预期结果：
1. 窗口 1600×900 出现，标题"可活动骨架手模 Demo"
2. 右侧面板完整显示（可滚动）
3. 手模型无彩色闪烁点，有 specular 高光
4. 录制按钮组可见，点击"开始录制"后时间计时
5. 停止后 `recordings/rec_*/imu_data.csv` 存在

---

## 结论

代码层面验收 **全部通过**（16/16 项）。编译和运行时验收因环境限制 **阻塞**，需在目标开发机上补执行。
